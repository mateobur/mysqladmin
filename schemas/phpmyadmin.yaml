class: 'EBox::MyAdmin'

enabledepends:
    - myadmin

models:
    - Options
    - OperationMode
    - RemoteServerConfiguration

composites:
    Backend: [myadmin/OperationMode, myadmin/RemoteServerConfiguration]

notifyactions:
    Options: [webserver/VHostTable]
