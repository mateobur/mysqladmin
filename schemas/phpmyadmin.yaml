class: 'EBox::MyAdmin'

enabledepends:
    - webserver

models:
    - Options
    - OperationMode
    - RemoteServerConfiguration

composites:
    Backend: [phpmyadmin/OperationMode, phpmyadmin/RemoteServerConfiguration]

notifyactions:
    Options: [webserver/VHostTable]
